def div():
    number = int(input("type the divisor number: "))
    size = int(input("type the maximum range: "))
    mylist = []
    for i in range(1, size + 1):
        if i % number == 0:
            mylist.append(i)
        else:
            continue
    return mylist


k = div()
print(type(k))
print(k)
